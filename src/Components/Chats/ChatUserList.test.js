import { render, screen } from "@testing-library/react";
import ChatUserList from "./ChatUserList";

describe("Test the components in ChatUserList component succesfully render", () => {
  test("", () => {});
});
